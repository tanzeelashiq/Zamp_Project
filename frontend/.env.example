# Used by server components (Next.js API routes, Server Components)
API_URL=http://localhost:4000

# Used by client components (browser-side fetches, EventSource)
NEXT_PUBLIC_API_URL=http://localhost:4000
